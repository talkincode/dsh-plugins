# AGENTS.md — dsh-plugins 开发规约

本仓库是 **DSH 插件集**：技能（skills）、MCP（Model Context Protocol）定义、以及未来的管理 UI。本文档告诉后续开发者（人类或 agent）如何在这个仓库里工作、目录怎么组织、边界在哪、提交前必须做什么。

在动手修改前，先读一遍 `docs/roadmap.md`（项目画像与方向），它是目标状态和非目标铁律的唯一事实来源；本文件只写"怎么工作"的硬规则，不重复画像内容。

## 仓库定位

- 这是一个**插件登记与分发仓库**，不是要发布到 npm 或任何公共包管理器的库。
- 三类资产各自独立、边界清晰：
  - **skills/**：可被 agent 运行时调用的技能定义，一技能一目录。
  - **mcp/**：MCP server / 工具的本地登记信息。
  - **（未来）管理 UI**：面向人类操作者，浏览/审阅上述登记项；当前阶段**不实现**，只在 `docs/roadmap.md` 里留方向。
- 宿主运行时（例如 Mira 或其他兼容 agent runtime）直接从仓库路径加载 `skills/`、`mcp/` 下的内容，不经过任何网络注册表。

## 目录约定

```text
dsh-plugins/
├── skills/<skill-id>/   # 一技能一目录，自包含（见下）
├── mcp/<mcp-id>/        # 一个 MCP 登记项一目录（约定，具体 schema 待定）
├── docs/roadmap.md      # 项目画像与方向（唯一事实来源）
├── AGENTS.md            # 本文件
└── README.md
```

新增顶层目录前，先确认它属于 skills / mcp / 管理面三类之一，或是纯文档性质（如 `docs/`）。不要在顶层随意新建与这三类无关的目录。

## 插件 / 技能 / MCP 边界

- **技能（skill）**：一个 `skills/<skill-id>/` 目录必须**自包含**——`SKILL.md` 加上它引用的脚本、资源、示例都放在同一目录下。禁止一个技能运行时依赖仓库其他目录里的私有资源才能工作。技能目录应当可以被整体复制到别处继续独立使用。
- **MCP 登记**：`mcp/` 目录下的内容是 MCP server/工具定义的本地登记信息，不是可执行服务本身的全部实现代码仓库（如果某个 MCP server 需要独立代码库，`mcp/` 下放的是登记/配置说明，指向该代码库）。
- **管理 UI**：当前阶段**不实现**任何管理 UI 代码。方向和约束写在 `docs/roadmap.md` 的"方向与意图"和"非目标"章节。若后续启动实现，不得提前锁定具体前端/后端技术栈。

## 分发约定

- **不使用 npm registry（或任何公共包管理器）作为分发通道。** 不建立 `npm publish` 流程，不把"发布到公共包管理器"当作目标或里程碑。
- 技能与 MCP 定义只通过**本地/仓库内加载**分发：宿主运行时直接读取 `skills/`、`mcp/` 下的文件路径。
- 新增/修改一个技能或 MCP 登记项，提交合并即生效，不需要额外的打包、发版、上传步骤。

## 提交规范

- 提交信息使用清晰的祈使句概括改动内容；纯文档/约定类改动应在提交信息中说明"文档/约定初始化，非功能实现"这类性质，避免被误读为功能交付。
- 每个 PR 只做一类改动（新增一个技能 / 新增一个 MCP 登记项 / 更新文档），避免把无关改动混在一起，便于审阅。
- 新增一级业务功能（例如真正落地一个技能或 MCP 登记项）时，必须同步在 `docs/roadmap.md` 的"验收矩阵"里登记对应行，并至少补上一条 Happy Path 端到端验证——否则改动视为不完整。

## 脱敏硬规则（MUST，不可降级为建议）

以下规则适用于本仓库的**任何提交、示例、日志、截图、fixture、Issue、PR、文档**：

1. **禁止提交真实密钥、token、cookie、密码。** 无论是代码里的常量、`.env` 示例文件，还是文档里的代码块，一律使用占位符（如 `<YOUR_TOKEN>`、`sk-xxxxxxxx`）。
2. **禁止提交内网地址清单、客户标识、账号信息。** 示例配置只能用假域名/假 IP（如 `10.0.0.0/8` 段的占位地址或 `example.internal`），不得使用真实内网拓扑或客户可识别信息。
3. **禁止在完整配置示例中保留真实的秘密字段。** 如果要展示一份完整配置作为示例，秘密字段（密钥、token、密码等）必须替换为占位符，不能只脱敏一部分。
4. **如果现有素材（截图、日志、fixture）含疑似秘密，禁止原样提交。** 提交前必须先脱敏处理；无法确认是否为真实秘密时，按"当作真实秘密处理"的保守原则处理。
5. **管理 UI（一旦实现）默认按脱敏展示连接/配置信息。** 名称、状态等非敏感信息可以直接显示；任何秘密值只能被写入（新增/更新配置），不能被读出或回显到界面上。

违反以上任意一条的提交/PR 应被直接拒绝或要求修改，不接受"先合并再补脱敏"的例外。

## 验收矩阵（硬性规定）

完整的业务能力覆盖矩阵维护在 `docs/roadmap.md` 的"验收矩阵"章节，本文件不重复矩阵内容，只固化以下覆盖底线为 **MUST 级**规则：

1. 每个一级功能至少有一条 Happy Path 的 E2E 验证。
2. 每个高风险功能（如脱敏检查、涉及资金/权限/外部副作用的操作）至少覆盖一条失败路径。
3. 每个涉及权限的功能至少验证两种角色（如管理员/普通用户，或有权/无权）。
4. 每个会修改系统状态的操作至少验证一次失败后的恢复或回滚。
5. **每次新增一级业务功能，必须同步新增对应的 E2E 并更新 `docs/roadmap.md` 的验收矩阵**，否则该变更视为不完整，不应合并。

## 与其他文档的关系

- `docs/roadmap.md`：目标状态、非目标铁律、方向与意图、验收矩阵——回答"做成什么样、不能做成什么样"。
- `AGENTS.md`（本文件）：回答"怎么工作"——目录、边界、提交规范、脱敏硬规则。
- 两者冲突时，以 `docs/roadmap.md` 的画像/铁律为准；本文件的操作性规则需与之保持一致，发现不一致应同步修订。
