# AGENTS.md — dsh-plugins 开发规约

本仓库是一个 **DSH 插件集合项目**：集中登记、发现、管理各类可被 agent 运行时（例如 Mira 或其他兼容宿主）使用的插件。它**不是**某一类特定资产（如 skills 或 MCP）的专属实现仓库，也不把"管理 skills 和 MCP"当作项目职责的全部——这两者只是初期规划的两类管理面能力。本文档告诉后续开发者（人类或 agent）如何在这个仓库里工作、目录怎么组织、边界在哪、提交前必须做什么。

在动手修改前，先读一遍 `docs/roadmap.md`（项目画像与方向），它是目标状态和非目标铁律的唯一事实来源；本文件只写"怎么工作"的硬规则，不重复画像内容。

## 仓库定位

- 这是一个**插件集合与登记仓库**，不是要发布到 npm 或任何公共包管理器的库，也不是 skills/MCP 的专属实现仓库。
- 插件可以来自不同类别（技能、MCP 工具、未来可能出现的其他扩展形态），但仓库对它们一视同仁：都通过同一套"一插件一目录"约定登记。
- **管理面（Skills 管理、MCP 管理 UI）是插件集合之上的附加能力**，不是项目本身的定位：它们负责登记信息的浏览与审阅，不代管插件的完整实现源码。
- 宿主运行时或管理面直接从仓库路径读取 `plugins/` 下的登记内容，不经过任何网络注册表。

## 目录约定

```text
dsh-plugins/
├── plugins/<plugin-id>/   # 一插件一目录，自包含登记（见下）
├── docs/roadmap.md         # 项目画像与方向（唯一事实来源）
├── AGENTS.md                # 本文件
└── README.md
```

新增顶层目录前，先确认它属于"插件集合（`plugins/`）"或"纯文档性质（如 `docs/`）"之一。不要为某一类插件（例如 skills 或 MCP）单独新建顶层目录（如 `skills/`、`mcp/`）作为其专属存放地——所有插件统一进 `plugins/`。

## 插件集合 vs 管理面边界

- **插件集合（`plugins/`）**：一个 `plugins/<plugin-id>/` 目录必须**自包含登记**——至少包含一个说明文件（如 `PLUGIN.md`），写清楚插件类型（skill / MCP / 其他）、如何被加载、真实实现指向哪里（本仓内联代码，或外部代码库链接）。
  - **登记优先于搬运**：如果插件实现已维护在独立代码库，`plugins/<plugin-id>/` 下放的是登记/配置说明，不必把该代码库的全部源码搬进来。只有足够小、天然适合内联维护的插件，才把完整实现放进这个目录。
  - 禁止以"把某类插件的实现整包落地本仓库"为目标去搬运外部代码库。
- **管理面（Skills 管理、MCP 管理 UI，未来实现）**：面向人类操作者，提供插件登记信息的浏览、发现、审阅能力（名称、类型、状态、指向）。当前阶段**不实现**任何管理面代码。方向和约束写在 `docs/roadmap.md` 的"方向与意图"和"非目标"章节。若后续启动实现：
  - 不得提前锁定具体前端/后端技术栈。
  - 不得把覆盖范围锁死为"只服务 skills 和 MCP 两类"，需要为未来插件类型的扩展留出空间。
  - 展示层默认隐藏秘密值，只有名称、状态、类型等非敏感信息可以直接可见。

## 分发约定

- **不使用 npm registry（或任何公共包管理器）作为分发通道。** 不建立 `npm publish` 流程，不把"发布到公共包管理器"当作目标或里程碑。
- 插件登记项只通过**本地/仓库内加载**分发：宿主运行时或管理面直接读取 `plugins/` 下的文件路径。
- 新增/修改一个插件登记项，提交合并即生效，不需要额外的打包、发版、上传步骤。

## 提交规范

- 提交信息使用清晰的祈使句概括改动内容；纯文档/约定类改动应在提交信息中说明"文档/约定修订，非功能实现"这类性质，避免被误读为功能交付。
- 每个 PR 只做一类改动（新增一个插件登记项 / 更新文档 / 新增管理面能力），避免把无关改动混在一起，便于审阅。
- 新增一级业务功能（例如真正落地一个插件登记项或一项管理面能力）时，必须同步在 `docs/roadmap.md` 的"验收矩阵"里登记对应行，并至少补上一条 Happy Path 端到端验证——否则改动视为不完整。

## 脱敏硬规则（MUST，不可降级为建议）

以下规则适用于本仓库的**任何提交、示例、日志、截图、fixture、Issue、PR、文档**：

1. **禁止提交真实密钥、token、cookie、密码。** 无论是代码里的常量、`.env` 示例文件，还是文档里的代码块，一律使用占位符（如 `<YOUR_TOKEN>`、`sk-xxxxxxxx`）。
2. **禁止提交内网地址清单、客户标识、账号信息。** 示例配置只能用假域名/假 IP（如 `10.0.0.0/8` 段的占位地址或 `example.internal`），不得使用真实内网拓扑或客户可识别信息。
3. **禁止在完整配置示例中保留真实的秘密字段。** 如果要展示一份完整配置作为示例，秘密字段（密钥、token、密码等）必须替换为占位符，不能只脱敏一部分。
4. **如果现有素材（截图、日志、fixture）含疑似秘密，禁止原样提交。** 提交前必须先脱敏处理；无法确认是否为真实秘密时，按"当作真实秘密处理"的保守原则处理。
5. **管理 UI（一旦实现）默认按脱敏展示连接/配置信息。** 名称、状态等非敏感信息可以直接显示；任何秘密值只能被写入（新增/更新配置），不能被读出或回显到界面上。

违反以上任意一条的提交/PR 应被直接拒绝或要求修改，不接受"先合并再补脱敏"的例外。

## 验收矩阵（硬性规定）

完整的业务能力覆盖矩阵维护在 `docs/roadmap.md` 的"验收矩阵"章节，本文件不重复矩阵内容，只固化以下覆盖底线为 **MUST 级**规则：

1. 每个一级功能至少有一条 Happy Path 的 E2E 验证。
2. 每个高风险功能（如脱敏检查、涉及资金/权限/外部副作用的操作）至少覆盖一条失败路径。
3. 每个涉及权限的功能至少验证两种角色（如管理员/普通用户，或有权/无权）。
4. 每个会修改系统状态的操作至少验证一次失败后的恢复或回滚。
5. **每次新增一级业务功能，必须同步新增对应的 E2E 并更新 `docs/roadmap.md` 的验收矩阵**，否则该变更视为不完整，不应合并。

## 与其他文档的关系

- `docs/roadmap.md`：目标状态、非目标铁律、方向与意图、验收矩阵——回答"做成什么样、不能做成什么样"。
- `AGENTS.md`（本文件）：回答"怎么工作"——目录、边界、提交规范、脱敏硬规则。
- 两者冲突时，以 `docs/roadmap.md` 的画像/铁律为准；本文件的操作性规则需与之保持一致，发现不一致应同步修订。
